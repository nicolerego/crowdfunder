<h1><%= @project.name %></h1>


<div>
	<h4>Project Description</h4>
	<p><%= @project.description %> </p>
</div>
<p>Owned and created by <%= @project.owner.first_name%></p>

<h4>Funding goal ($): <small><%= @project.funding_goal %></small></h4>

<h4>Days to go: <small> <%= distance_of_time_in_words(Time.now, @project.end_date) %> </small>days</h4>

<p>Start date: <%= @project.start_date %></p>
<p>End date: <%= @project.end_date %></p>


<%= link_to "Edit this Projects", edit_project_path %> | 
<%= link_to "Delete this Project", @project, data: {confirm: "Are you sure you want to delete this project?"}, :method => :delete %>

<h4>Rewards:</h4>
<p></p>

<% @project.rewards.each do |reward| %>
  <p><%= reward.title %> <%= link_to "Check it out!", "/projects/#{@project.id}" %></p>
  <p>Description: <%= reward.description %></p>
  <p>Amount ($): <%= reward.amount %></p>
<% end %>